include: package:flutter_lints/flutter.yaml

# Standard Flutter lint set keeps CI signal meaningful without failing on style
# nitpicks. Add project-specific rules here as the codebase matures.
analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
